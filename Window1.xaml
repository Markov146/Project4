<Window x:Class="WpfApp5.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp5"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="70"/>
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300*"/>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="300*"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="input" Grid.ColumnSpan="3" Margin="0,160,0,0" Grid.RowSpan="2"/>
        <TextBlock  FontSize="30" HorizontalAlignment="Left" Margin="100,90,0,0" TextWrapping="Wrap" Text="Введите тип нужной добавляемой записи" VerticalAlignment="Top" Grid.ColumnSpan="3" Grid.RowSpan="1" Width="641" Height="40"/>
        <Button 
            x:Name="OkButton"
            Grid.Column="1" Grid.Row="2"
            Content="Ок" Click="Button_Click" FontSize="25" Margin="0,15,0,15" Grid.RowSpan="2"/>
    </Grid>
</Window>


